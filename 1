numbers = [-40, -30, -10, 10, 20, 80]
three_sum = []
count = 0
for i in numbers:
  for j in numbers:
    for k in numbers:
      if i+j+k == 0:
        three_sum.append((i, j, k))
        count += 1
print(three_sum, '\ncount:', count)

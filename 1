import random

numbers = []
for x in range(100):
  numbers.append(random.randint(-100, 100))
three_sum = []
count = 0
for i in numbers:
  for j in numbers:
    for k in numbers:
      if i+j+k == 0:
        three_sum.append((i, j, k))
        count += 1
print(three_sum, '\ncount:', count)
